node_modules/
ui/dist/
*.log
.vite/
coverage/
.claude/worktrees/
.screenshots/

# Generated video. Clips are the user's own files and a World copies in what it needs;
# none of it belongs in the repo.
server/src/live/clips/
*.mp4
*.webm
*.m4v

# Voice models: ~353MB, fetched on first use and verified against a digest.
# A checkout may hold its own copy here; HAL_VOICE_MODELS_DIR points elsewhere.
.voice-models/
