// Placeholder shell — the full HAL UI lands in U4.
export function App() {
  return (
    <main style={{ background: "#0a0a0a", color: "#e6e6e6", minHeight: "100vh", display: "grid", placeItems: "center", fontFamily: "monospace" }}>
      <p>HAL 1000 — all systems nominal.</p>
    </main>
  );
}
