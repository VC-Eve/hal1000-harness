{
  "name": "hal1000-harness",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "server",
    "ui"
  ],
  "scripts": {
    "dev:server": "npm run dev --workspace @hal1000/server",
    "dev:ui": "npm run dev --workspace @hal1000/ui",
    "build": "npm run build --workspace @hal1000/ui",
    "start": "npm run start --workspace @hal1000/server",
    "test": "vitest run",
    "typecheck": "tsc -p server/tsconfig.json --noEmit && tsc -p ui/tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/ws": "^8.5.13",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
